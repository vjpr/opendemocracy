html
head
  meta(charset="utf8")
  title Tests
  script(src='https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js')
  != css('test.css')
body
  #mocha
  != js('test.js')
  :coffeescript
    should = chai.Should()
    expect = chai.expect
    mocha.setup
      ui: 'bdd'
      globals: []
    $(document).ready -> mocha.run()
